[package]
name = "wist-validate"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
rust-version = "1.85"

[dependencies]
serde_json = "1.0"
time = { version = "0.3", features = ["formatting", "parsing"] }
toml = "0.8"
wist-contracts = { version = "0.1.1", path = "../wist-contracts" }

[lib]
path = "src/lib.rs"
